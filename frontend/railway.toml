# Railway configuration for Provera Frontend
[build]
builder = "nixpacks"

[build.nixpacks]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve -s dist -l $PORT"

[envs]
NODE_ENV = "production"
